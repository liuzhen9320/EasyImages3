# Security Audit — EasyImage3

> 审计日期: 2026-07-09
> 代码版本: 2.8.7 (master `42b4711`)

## 第二轮复审（2026-08-29，基于 `04025cc`）

> 状态说明：以下为第一轮 19 项修复完成后新确认的问题，均待修复。按“确认一项、立即记录一项”的方式持续补充。

### 20. 严重 — `admin/filer.php` 可越出站点根目录且写操作无 CSRF（已修复）

**文件**: `admin/filer.php:22`, `admin/filer.php:83-90`, `admin/filer.php:627-634`, `admin/filer.php:678-884`, `admin/filer.php:955-958`

旧版 web-indexr 文件管理器把根目录设为 `$_SERVER['DOCUMENT_ROOT']`，但相对路径转换仅做字符串拼接：

```php
RexHelper::$root = $_SERVER['DOCUMENT_ROOT'];

static function path_rtoa($path)
{
    $path = self::$root . DIRECTORY_SEPARATOR . trim($path, '/\\');
    return $path;
}
```

`path` 来自 `$_REQUEST`，没有 `realpath()`、`..` 拒绝或根目录前缀校验。登录文件管理器后，攻击者可提交 `path=/../../tmp`，通过 `newfile`、`edit`、`upload`、`rename`、`chmod`、`zip`、`unzip`、`delete` 等操作读写或删除站点根目录外、PHP 进程权限可及的文件。

同时，所有动作仅检查文件管理器 Session，不校验 CSRF Token；删除、注销和清空 OPcache 等动作可由 GET 触发。实测使用 `POST ?action=newfile&path=/../../tmp` 成功创建 `/tmp/easyimages-filer-audit-20260829.txt`，再用不含 Token 的 `GET ?action=delete&path=/../../tmp/easyimages-filer-audit-20260829.txt` 成功删除。

**修复要求**：将文件管理根目录固定到配置的图片存储根；对每个源/目标路径做规范化和目录边界校验，拒绝 `..`、NUL 与符号链接逃逸；所有状态变更仅接受 POST 并统一校验 CSRF Token；移除 GET 写操作。考虑该文件管理器长期无人维护，优先评估删除 `admin/filer.php`，只保留已受限的 `admin/manager.php`。

### 21. 严重 — 安装控制器可被无参数 GET 触发并锁定为公开默认凭据（已修复）

**文件**: `install/contorl.php:4-31`, `config/config.php:13-16`, `app/function.php:289-297`

发行包不包含 `config/install.lock`。安装控制器虽然按字段读取 POST，但不要求请求方法为 POST，也不要求任何安装流程状态；无论请求是否带参数，都会执行：

```php
$config_file = APP_ROOT . '/config/config.php';
cache_write($config_file, $config);
file_put_contents(APP_ROOT . '/config/install.lock', '安装程序锁定文件。');
```

因此，攻击者只需在站点完成安装前访问一次 `/install/contorl.php`，就能跳过环境检查和设置表单，使用发行包配置创建安装锁。发行包中的账号为 `admin`，密码值是公开默认密码 `admin@123` 的 SHA-256；登录页正好提交 SHA-256，`_login()` 又直接比较该值，攻击者随后可以默认凭据取得管理员权限。

在临时检出的干净副本中实测，无参数 GET 返回 HTTP 200，`config/install.lock` 从不存在变为存在，响应显示“安装成功”，最终配置满足 `user=admin` 且 `password=sha256('admin@123')`。

**修复要求**：安装提交只接受 POST；通过服务端 Session 保存并验证上一步产生的一次性安装 Token；拒绝空请求和缺失必填字段；发行包不得包含可登录的固定凭据；写配置与创建锁应使用原子、失败即回滚的流程。部署层应在安装完成后移除或禁止访问整个 `install/` 目录。

### 22. 高危 — 正常安装写入 bcrypt，但登录协议仍比较 SHA-256，管理员必然无法登录（已修复）

**文件**: `install/contorl.php:8-12`, `admin/index.php:146-150`, `admin/index.php:182-191`, `app/function.php:289-297`

安装表单直接提交明文密码，控制器用 `password_hash()` 保存 bcrypt：

```php
$config['password'] = password_hash($_POST['password'], PASSWORD_DEFAULT);
```

但管理员登录页仍在浏览器中执行 `SHA256(password.value)`，服务端 `_login()` 没有调用 `password_verify()`，而是把收到的 64 位 SHA-256 与配置中的 bcrypt 字符串直接全等比较：

```php
if ($user === $config['user'] && $password === $config['password']) {
```

两种格式永远不可能相等，导致通过正常安装流程设置的管理员凭据不可用。临时副本实测：POST 安装后密码以 `$2y$` 开头且能由 `password_verify()` 验证明文；随后按登录页协议提交该密码的 SHA-256，页面返回“密码错误”。

**修复要求**：统一密码协议。推荐在 HTTPS 下提交明文密码并在服务端使用 `password_verify()`；认证 Cookie 改为随机服务端 Session 标识，不再保存任何密码或密码哈希。对现有 64 位 SHA-256 配置提供一次成功登录后的渐进迁移，管理员修改密码、安装和登录必须共用同一套散列与验证函数，并增加从安装到首次登录的端到端测试。

**修复状态**：登录、安装、管理员改密和上传者账号统一使用服务端密码散列函数；浏览器只保存随机 PHP Session ID，旧 SHA-256 配置会在成功登录后自动迁移。CI 已覆盖两阶段安装、首次登录、Session 轮换、旧 Cookie 清除与旧散列迁移。

### 23. 高危 — `admin/manager.php` 的文件写操作仍无 CSRF，且创建/删除/改名使用 GET（已修复）

**文件**: `config/config.manager.php:22`, `admin/manager.php:311-392`, `admin/manager.php:443-653`, `admin/manager.php:656-870`

EasyImage 的管理器配置把 TinyFileManager 自带认证关闭，因此通过入口处的全站管理员 Cookie 校验后即可执行全部文件操作。管理器没有接入项目的 `csrf_validate()`；保存、上传、复制等 POST 动作不校验 Token，创建、删除、复制、移动、改名等动作甚至由 GET 参数直接触发：

```php
if (isset($_GET['del']) && !FM_READONLY) { /* 删除 */ }
if (isset($_GET['new'], $_GET['type']) && !FM_READONLY) { /* 创建 */ }
if (isset($_GET['ren'], $_GET['to']) && !FM_READONLY) { /* 改名 */ }
```

认证 Cookie 的 `SameSite=Lax` 不能防住顶层导航发起的跨站 GET。攻击者可诱导已登录管理员打开特制链接，在配置的图片根目录内创建、删除、改名或移动文件；这会造成图片批量破坏，还可能把已有文件改成可由 Web 服务器主动解释的扩展名。

临时副本实测：仅携带合法管理员 Cookie、完全不提交 CSRF Token，访问 `?p=&type=file&new=manager-csrf-audit-20260829.txt` 成功在 `i/` 创建文件；随后访问 `?p=&del=manager-csrf-audit-20260829.txt` 成功删除，两次响应均为 302。

**修复要求**：所有状态变更统一限制为 POST，并在动作分发前校验项目 CSRF Token；GET 只允许查看和下载。表单、AJAX、分片上传均应使用同一 Token 机制。增加请求方法与 CSRF 的回归测试，并升级长期停留在 2.4.7 的 TinyFileManager，或以项目内受维护的最小文件管理功能替代。

**修复状态**：项目入口对所有文件管理器 POST 请求统一校验 CSRF Token，旧 GET 写操作返回 405；创建、删除、重命名、复制移动、解压、编辑、设置、AJAX 和分片上传均已接入同一 Token。回归测试覆盖普通请求、AJAX 与分片上传的拒绝和成功路径。本项目继续维护现有深度定制文件管理器，避免直接替换上游 2.6 导致认证、存储根目录和静态资源集成回归。

### 24. 高危 — BootCSS/BootCDN 投毒事件引发全球开发者供应链信任危机（已修复）

**文件**: `docs/index.html:15`, `docs/index.html:83-93`

文档站的 docsify 主题、主脚本和三个插件共 5 个资源原先由 `cdn.bootcdn.net` 加载。BootCSS/BootCDN 投毒事件使其成为前端供应链风险源，并引发全球开发者对该 CDN 资源完整性与运营可信度的信任危机；继续引用会让文档访问者执行无法再被项目方信任的第三方内容。现已按要求全部替换为相同 docsify 4.13.0 版本的 `cdnjs.cloudflare.com` HTTPS 地址，资源顺序和版本保持不变。

验证结果：全仓已无 `bootcss`、`bootcdn` 或 `cdn.bootcdn.net` 引用；5 个 cdnjs 目标均返回 HTTP 200 且 Content-Type 正确，本地 `/docs/` 返回 HTTP 200。

### 25. 高危 — `admin/manager.php` 远程上传的 SSRF 过滤可被整数 IP 和私网地址绕过（已修复）

**文件**: `admin/manager.php:566-640`

TinyFileManager 的“从 URL 上传”功能由服务器主动请求用户提供的 HTTP(S) 地址。现有防护只按原始主机字符串拦截 `localhost`、`127.*`、`::1`，并只禁止 4 个端口：

```php
$domain = parse_url($url, PHP_URL_HOST);
if (preg_match('/^localhost$|^127...$|...::1$/i', $domain) || in_array($port, $knownPorts)) {
    exit();
}
copy($url, $temp_file, $ctx);
```

代码没有解析 DNS 后的全部 A/AAAA 地址，也没有拒绝 RFC1918 私网、链路本地、云元数据、保留地址和非标准形式的回环地址；PHP HTTP 包装器还可能跟随未经重新校验的重定向。拥有管理器请求能力的攻击者可借此探测或读取 PHP 进程可访问的内网 HTTP 服务，并把响应保存到公开图片目录。

实测将回环地址 `127.0.0.1` 写成整数 `2130706433` 后，`http://2130706433:28773/public/images/404.png` 未被过滤，接口返回 `{"done":{"name":"404.png"}}`；落盘文件与本机 28773 服务返回的原文件逐字节一致。

**修复要求**：若业务不必需，禁用 URL 上传。否则只允许 HTTP/HTTPS，解析并固定连接目标，对每个 IPv4/IPv6 地址使用 `FILTER_FLAG_NO_PRIV_RANGE | FILTER_FLAG_NO_RES_RANGE` 等完整策略校验；拒绝解析结果变化、用户信息、异常端口和非规范 IP；每次重定向都重新解析与校验，限制跳转次数、响应体大小和超时，并避免把响应直接保存到可公开访问目录。

**修复状态**：已移除远程 URL 上传的服务端抓取实现、上传页入口和前端脚本。旧客户端提交该动作会收到 HTTP 410，服务器不会解析或访问目标 URL；回归测试使用整数形式的回环地址确认请求未到达本地目标服务且未产生落盘文件。本地文件上传功能不受影响。

### 26. 高危 — 无条件信任 `X-Forwarded-For`，上传 IP 黑白名单和游客配额可绕过（已修复）

**文件**: `app/function.php:1462-1536`, `app/function.php:1915-1958`, `app/upload.php:49-76`, `api/index.php:30-40`

`real_ip()` 无论直连来源是否为可信反向代理，都优先采用客户端提供的 `X-Forwarded-For` 或 `Client-IP`，并从中提取第一个看似 IPv4 的片段。上传 IP 黑白名单、网页游客每日上传次数和上传审计日志都依赖这个结果。

任意远程客户端都可以自行设置请求头来轮换 IP、绕过黑名单与每日配额，或冒充白名单地址；日志中的攻击来源也会被污染。该函数还不支持 IPv6，合法 IPv6 地址会归并为 `0.0.0.0`，造成不同用户共享配额和误封。

函数级实测：连接地址固定为 `127.0.0.1` 时，黑名单 `127.0.0.1` 正常阻止；加入 `X-Forwarded-For: 203.0.113.77` 后 `real_ip()` 返回伪造值，黑名单判定变为放行。连接地址为 `198.51.100.20` 时，伪造 `X-Forwarded-For: 192.0.2.10` 也能通过仅允许 `192.0.2.10` 的白名单。

**修复要求**：默认只使用经过 `FILTER_VALIDATE_IP` 验证的 `REMOTE_ADDR`。新增显式可信代理网段配置；仅当直连地址属于可信代理时，才按代理约定从右向左解析转发链，并完整支持 IPv4/IPv6。黑白名单、游客配额、API/登录限流和日志必须统一使用同一个可信客户端地址函数，并增加伪造头、代理链和 IPv6 测试。

**修复状态**：默认忽略客户端提供的转发头，只使用规范化后的直连地址。新增 IPv4/IPv6 精确地址与 CIDR 可信代理配置；仅直连来源受信时按 `CF-Connecting-IP`、`X-Real-IP`、`X-Forwarded-For` 的固定优先级取值，前两者拒绝逗号和畸形值，XFF 从右向左解析代理链，旧式 `Client-IP` 不再使用。上传黑白名单、游客配额、上传/登录日志、API/登录限流、分片桶、文件管理器和 TimThumb 调试记录均使用统一结果。黑白名单同时修复了显式 IP 参数和通配白名单逻辑，并支持 IPv6/CIDR。

### 27. 严重 — 图片删除与回收路径可穿越存储目录（已修复）

**文件**: `app/function.php:946-1003`, `app/function.php:1317-1369`, `app/del.php:23-64`, `app/del.php:88-193`

删除函数只检查原始字符串中是否包含配置的图片目录：

```php
if (is_file($url) && strrpos($url, $config['path'])) {
    unlink($url);
}
```

`/i/../../config/config.php` 同时满足子串检查和 `is_file()`，文件系统解析后却位于图片目录之外。该路径可从管理员单文件/批量删除、回收与恢复入口传入；拥有合法删除令牌的路径也没有独立边界校验。版本更新动作还会直接执行 `unlink(APP_ROOT . $_POST['url'])`，形成另一处任意文件删除入口。FTP 同步删除会继续接收未经验证的路径。

**修复要求**：统一解析 URL 与相对路径，拒绝 NUL、反斜杠逃逸、`.`/`..` 段和非存储目录前缀；对现有文件使用 `realpath()` 验证最终目标仍位于存储根目录，并拒绝符号链接逃逸。只有路径验证成功后才能执行本地或 FTP 删除。回收与恢复的源、目标目录都必须做同样的边界检查；版本更新只能删除固定的版本缓存文件。

**修复状态**：新增统一存储路径规范化和文件解析函数，普通删除、批量删除、加密删除链接、回收、恢复及 FTP 同步均经同一边界校验。回收目录仅允许 `recycle/` 和 `suspic/`，恢复目标会验证父目录的真实路径且拒绝覆盖已有文件；版本操作固定为 `admin/logs/version/version.json`。回归测试覆盖有效加密令牌携带穿越路径、双重编码入口、符号链接、正常删除、回收恢复和版本缓存删除。

### 28. 高危 — 上传日志月份可造成路径穿越和本地 PHP 文件包含（已修复）

**文件**: `app/viewlog.php:9-37`, `admin/admin.inc.php:589-619`

上传日志查看器把 POST 的 `logDate` 直接拼接到 `admin/logs/upload/` 后，再通过 `require_once` 加载：

```php
$logFile = APP_ROOT . '/admin/logs/upload/' . $_POST['logDate'] . '.php';
require_once $logFile;
```

管理员会话中的请求可提交 `../../../config/file` 越出日志目录并执行任意已有 PHP 文件。入口另行要求 `md5($config['password'] . date('ymdh'))`，但该值直接嵌入后台表单和 URL，既不能替代 Session 鉴权，又会把密码派生材料暴露给浏览器历史、访问日志和可能的引用来源。

**修复要求**：只接受严格的 `YYYY-MM` 月份并限制月份为 `01` 至 `12`，从固定上传日志目录选择对应文件；继续以管理员服务端 Session 作为唯一访问凭据，移除表单和 URL 中的密码派生签名。

**修复状态**：`logDate` 现在必须匹配合法月份格式，否则返回 HTTP 400 且不会包含任何文件。上传日志和登录日志仅依赖管理员 Session；后台不再生成或传递小时签名。回归测试覆盖未认证访问、穿越载荷、非法月份、正常月份和登录日志入口。

### 29. 高危 — 源图保护解密路径缺少存储边界校验（已修复）

**文件**: `app/hide.php:12-31`

源图保护入口验证加密令牌后，直接把明文路径拼到站点根目录并读取：

```php
$real_path = APP_ROOT . urlHash($_GET['key'], 1, $config['hide_key']);
echo file_get_contents($real_path);
```

加密认证只能证明令牌由持有密钥的一方生成，不能证明解密内容属于图片目录。一旦错误签发、密钥泄露或其他服务端入口产生了带穿越段的有效令牌，攻击者就能读取 PHP 进程可访问的任意站点文件；符号链接也可把看似位于 `i/` 的路径指向目录外。

**修复要求**：解密后必须使用与删除接口相同的存储路径规范化和 `realpath()` 边界校验，拒绝目录穿越与符号链接逃逸；无效令牌和越界目标统一返回 404 图片，且不得泄露目标内容。

**修复状态**：源图入口把解密结果交给 `resolve_storage_file()`，只输出图片存储根目录内的普通文件。无效、篡改、穿越和符号链接令牌均返回 HTTP 404 及固定占位图。回归测试同时验证合法源图令牌仍可读取。

### 30. 中危 — 后台批量压缩目录可穿越图片存储根并原地覆盖站点外图片

**文件**: `app/compressing.php:20-40`, `app/compress/function.compress.php:15-46`

批量压缩入口虽然要求管理员 Session 和 CSRF Token，但它对提交的 `folder` 只做一次 `urldecode()`，随后直接拼接到图片目录：

```php
$getFolder = urldecode($_POST['folder']);
$folder = '..' . $config['path'] . $getFolder;
compress($folder, $type, $source);
```

服务端没有使用已有的 `resolve_directory_within()`，也不拒绝 `..` 或符号链接。攻击者只要能以管理员身份提交请求，就可把 `folder` 设为 `..` 或更深的穿越路径，使 `Imgcompress` 递归枚举图片存储根以外的目录，并通过 `compressImg($boxImg)` 原地覆盖其中的图片；`TinyPng` 分支又使用未经约束的原始 `source` 重建路径，存在相同问题。页面的只读日期输入不构成服务端安全边界。

**修复要求**：把 `folder` 限制为严格的日期目录格式，或统一交给 `resolve_directory_within(APP_ROOT . $config['path'], ...)` 解析；拒绝存储根本身、目录穿越和符号链接逃逸。`compress()` 应只接受已经规范化且确认位于存储根内的绝对目录，并在覆盖每个文件前再次验证真实路径。增加 `..`、双重编码、绝对路径、符号链接和正常日期目录的回归测试。

### 31. 中危 — 广场单图删除与回收没有提交 CSRF Token，管理员操作必然失败

**文件**: `app/list.php:176-217`, `app/del.php:75-79`

删除入口修复 CSRF 后要求所有管理员写请求同时满足 POST 和有效 `_csrf`：

```php
if ($_SERVER['REQUEST_METHOD'] !== 'POST' || !csrf_validate(...)) {
    http_response_code(403);
    exit('Invalid CSRF token');
}
```

但广场页面的单图 `ajax_post()` 仍只发送 `url` 与 `mode`，没有像同页批量操作和 `app/info.php` 那样附带 `_csrf`。管理员点击单张图片的“删除”或“回收”后，服务端必然返回 403；回调随后对 `Invalid CSRF token` 执行 `JSON.parse()`，前端还会抛出异常，无法给出正确结果提示。

**修复要求**：在广场单图请求中加入 `csrf_token()`，并让接口统一返回带 `JSON_UNESCAPED_UNICODE` 的 JSON 错误；前端应根据 HTTP 状态和结构化响应显示失败，而不是无条件 `JSON.parse()`。增加管理员从广场执行单图删除、单图回收及无 Token 拒绝的端到端测试。

### 32. 高危 — 图片详情参数可闭合内联脚本造成公开反射型 XSS

**文件**: `app/info.php:9-23`, `app/info.php:283-285`

详情页把公开 GET 参数 `img` 或 `history` 仅交给 `strip_tags()`，然后把得到的 `$getIMG` 原样插入双引号 JavaScript 字符串：

```php
$getIMG = strip_tags($_GET['img']);
// ...
document.title = "图片<?php echo basename($getIMG); ?>的详细信息 - <?php echo $config['title']; ?>"
```

`strip_tags()` 不会移除引号、反斜杠或 JavaScript 语法。即便目标文件不存在，代码只替换 `$imgABPath` 和 `$img_url`，并未重置 `$getIMG`，所以攻击者无需上传任何文件即可使用形如 `?img=";alert(document.domain);//` 的参数闭合字符串并执行任意脚本。默认 `show_exif_info=1` 允许未登录访问；若管理员打开恶意链接，脚本可读取页面中的 CSRF Token，并以管理员权限发起同源写操作。

**修复要求**：先用 `normalize_storage_web_path()`/`resolve_storage_file()` 将输入限制到真实图片文件；所有 HTML 文本和属性按上下文使用 `htmlspecialchars(..., ENT_QUOTES, 'UTF-8')`，进入 JavaScript的值必须用 `json_encode()`，不再拼接脚本文本。优先把标题直接由 PHP 输出到 `<title>` 或用 `textContent` 设置。增加引号、反斜杠、换行、`</script>`、不存在文件及合法图片路径的回归测试。

### 33. 高危 — 广场日期参数可穿越目录并触发未认证的递归文件系统扫描

**文件**: `app/list.php:16-43`, `app/function.php:558-583`, `app/function.php:704-715`, `app/function.php:781-822`

广场把 `date` 仅与最早允许日期做字符串大小比较，所谓日期过滤正则又漏写了所有 `\d` 的反斜杠，且格式与实际使用的 `Y/m/d/` 不一致：

```php
$path = preg_replace("/^d{4}-d{2}-d{2} d{2}:d{2}:d{2}$/s", "", trim($path));
$fileArr = get_file_by_glob(APP_ROOT . config_path($path) . $fileType, 'list');
$httpUrl = array('date' => $path, 'num' => getFileNumber(APP_ROOT . config_path($path)));
```

因此以 `cache/` 或一个已存在日期目录开头、后接 `../` 的值可以通过字符串比较，并让 `glob()` 越出图片存储根。页面可泄露目标目录中的文件名；更严重的是 `getFileNumber()` 会无深度、文件数和时间上限地递归遍历目标。攻击者可让公开请求扫描站点根、其父目录或其他 PHP 进程可读的大型目录，持续消耗 CPU、磁盘 I/O 和请求工作进程。`config_path()` 还会对同一用户路径尝试 `mkdir()`/`chmod()`，进一步扩大了副作用。

**修复要求**：`date` 只接受 `\AY/m/d/\z` 对应的严格数字格式，并用 `DateTime::createFromFormat()` 验证真实日历日期及允许的起止范围；解析后的目录必须再经过存储根真实路径边界检查。查询函数不得创建或修改目录，并应限制递归深度、文件数和执行时间。增加未来日期、无效日期、`..`、编码穿越、符号链接以及超大目录的测试。

### 34. 中危 — 预生成缩略图会改写上传对象状态，水印、压缩和日志随后错误作用于缩略图

**文件**: `app/upload.php:166-264`, `api/index.php:124-222`, `app/class.upload.php:3095-3106`

上传成功后，代码先用 `$pathIMG` 保存原图相对路径；当 `thumbnail === 2` 时又在同一个 `Upload` 对象上调用第二次 `process()`。上传类在每次处理开始都会清空并重建 `file_dst_name`、`file_dst_pathname` 等目标字段，因此第二次调用结束后这些字段已经指向 `i/cache/` 中的缩略图：

```php
$handle->process(APP_ROOT . $config['path'] . 'cache/');
// ...
write_upload_logs($pathIMG, ..., $handle->file_dst_pathname, ...);
water($handle->file_dst_pathname);
process_compress($handle->file_dst_pathname);
```

Web 与 API 端点随后都用被改写的路径写日志、加水印和压缩。结果是对外返回的原图没有执行所配置的水印和二次压缩，上传日志的 MD5 却来自缩略图；若后续逻辑依赖该 MD5 或处理状态，原图与审计记录不一致。

**修复要求**：第一次原图处理成功后立即保存不可变的原图绝对路径、文件名和大小；缩略图处理使用另一个对象或只把缩略图结果写入独立变量。所有日志、审核、水印、压缩和远端上传明确使用原图变量。合并 Web/API 共用的后处理函数，并测试 `thumbnail=0/1/2` 与水印、压缩、日志组合。

### 35. 中危 — 游客每日上传配额存在并发竞态，可突破限制并丢失计数/上传日志

**文件**: `app/upload.php:64-76`, `app/upload.php:250-256`, `app/function.php:1606-1668`, `app/function.php:2200-2250`, `app/function.php:2278-2321`

游客请求在上传前通过 `get_ip_upload_log_counts()` 只读检查计数，文件处理完成并已经输出成功响应后，才由 `write_ip_upload_count_logs()` 再次读取整个 PHP 数组、加一并覆盖文件。这不是一个原子的“检查并占用”操作：多个并发请求可以同时读到相同旧值、全部通过限制，最后都写回同一个 `旧值 + 1`，从而用一次计数完成多次上传。

底层 `writefile()` 也在 `fopen($filename, 'w')` 已截断文件之后才调用 `flock()`；文件锁不能阻止另一个进程在等待锁之前截断内容。相同的读-改-写模式还用于月度上传日志，并发上传可能丢失彼此的日志项，读取方还可能短暂包含空的 PHP 文件。

**修复要求**：为配额实现单一的原子函数，在独占锁内读取、清理、判断并预占计数，超限时不落盘；上传最终失败则显式释放预占。文件型状态写入使用同目录临时文件加 `LOCK_EX`、`fflush()` 和原子 `rename()`，或在打开非截断模式并取得锁后才 `ftruncate()`。上传日志也需在同一锁内合并，不能使用通用的无事务全量覆盖。增加超过配额的并发上传和并发日志完整性测试。

### 36. 中危 — `config_path()` 把 Web 路径当作文件系统绝对路径，在服务器根目录检查和创建目录

**文件**: `config/config.php:31-32`, `app/function.php:558-583`, `app/upload.php:151-167`, `api/index.php:118-125`

默认 `$config['path']` 是以斜杠开头的 Web 路径 `/i/`。`config_path()` 直接对 `$config['path'] . $path` 调用 `is_dir()`、`mkdir()`、`is_writable()` 和 `chmod()`，所以它实际检查和修改的是文件系统根目录下的 `/i/Y/m/d/`。函数返回后，上传端点才在外层拼接 `APP_ROOT`，真正落盘目标则是 `APP_ROOT/i/Y/m/d/`。

这导致辅助函数的权限检测和修复完全作用于错误目录。普通非特权 PHP 会在每次页面/上传请求中静默失败，掩盖真实存储目录权限问题；以 root 运行的容器或开发服务器则会意外创建并修改 `/i/`。真正的应用目录目前只是依赖第三方上传类的 `dir_auto_create` 才得以创建，其他调用者不能得到函数契约承诺的结果。

**修复要求**：明确区分规范化 Web 路径与绝对文件系统路径；所有目录存在性和权限操作都针对 `APP_ROOT . $webPath`，返回值则保持为 Web 路径。禁止用户输入参与目录创建，并让只读查询使用无副作用的路径解析器。增加非站点工作目录、无权限存储目录、首次上传和容器 root 用户场景测试。

### 37. 严重 — 安装 Token 只验证步骤顺序，公开未安装实例仍可被任意远程访问者抢先接管

**文件**: `install/index.php:113-133`, `install/install.php:9-22`, `install/contorl.php:13-66`, `install/functions.php:16-54`, `tests/authentication_flow.sh:34-58`

两阶段安装 Token 修复了无参数请求直接落锁的问题，但 Token 由公开的 `install/index.php` 无条件签发给当前访客。任何远程客户端都能建立自己的 Session、读取环境 Token、换取配置 Token，再提交自选域名、管理员账号和密码。Token 仅证明同一客户端按顺序访问了页面，没有证明客户端是部署者。

仓库的认证流程测试完整演示了这一无需预共享凭据的 HTTP 流程。若新实例在管理员完成安装前可以从网络访问，先到的攻击者即可写入其密码并创建 `install.lock`，获得永久管理员权限。配置页默认勾选 `del_extra_files`，攻击者安装时还会删除 `.git`、`.github`、文档、日志和安全说明。

**修复要求**：默认只允许 CLI 安装，或要求部署时通过环境变量/只读文件提供一次性高熵安装密钥，并在每一步服务端校验；可选地仅允许经明确配置的管理网段访问。安装前所有非安装入口应返回 503，反向代理部署文档必须要求先隔离网络完成安装。安装密钥成功使用后立即销毁，安装完成后禁止整个 `install/`。增加无密钥远程安装失败、正确密钥成功、并发抢占和安装前其他入口不可用的测试。

### 38. 高危 — TLS 终止代理后方的认证 Session Cookie 会缺少 `Secure` 标志

**文件**: `app/function.php:76-105`, `app/function.php:382-390`, `app/function.php:1738-1838`

项目支持通过 `trusted_proxies` 识别真实客户端 IP，但 Session 初始化判断 HTTPS 时只检查直连的 `$_SERVER['HTTPS']` 和 `SERVER_PORT === 443`。在常见的 Nginx、Ingress 或负载均衡器终止 TLS、再以 HTTP 连接 PHP 的部署中，这两项都表示 HTTP，即使可信代理已发送 `X-Forwarded-Proto: https`，`easyimage_session` 仍会以 `secure=false` 设置。

该 Cookie 承载管理员和上传者认证，生命周期为 14 天。只要同一域名仍可通过 HTTP 访问，浏览器就可能在明文请求中发送未标记 Secure 的 Session ID，使网络攻击者可劫持会话。`admin/manager.php` 反而无条件信任 `X-Forwarded-Proto` 来生成 URL，说明当前协议识别既不统一，也没有复用可信代理边界。

**修复要求**：新增统一的 `request_is_https()`：直连 HTTPS 始终可信；仅当 `REMOTE_ADDR` 属于 `trusted_proxies` 时才解析格式严格的转发协议头，并拒绝多值或未知协议。Session 初始化、Cookie 清理、绝对 URL 生成和文件管理器全部复用它。生产环境可提供强制 `cookie_secure` 配置并启用 HSTS。增加直连 HTTPS、可信代理 HTTPS、伪造头、不可信代理和多级代理测试。

### 39. 中危 — 配置更新白名单遗漏 `path`，后台修改存储目录会静默失效

**文件**: `admin/admin.inc.php:40-75`, `admin/admin.inc.php:387-396`, `app/function.php:188-209`

上传设置表单公开了可编辑的 `<input name="path">` 并与 `storage_path` 一起提交 `update`。安全修复引入的 `$allowed_config_keys` 包含 `storage_path`，却遗漏了 `path`；`filter_config_update()` 会通过 `array_intersect_key()` 静默删除该字段。处理器不检查实际变化或写入结果，仍显示“保存成功”。

因此管理员在受支持的 UI 中修改图片存储根后，配置保持旧值，页面刷新才恢复；依赖新路径的 Web 服务器配置、迁移或分离部署会出现链接与落盘位置不一致。该行为也与项目文档中 `path` 可配置的契约冲突。

**修复要求**：为 `path` 增加专门的服务端规范化规则后再纳入白名单：只接受站点内绝对 Web 目录，要求首尾单斜杠，拒绝 `.`/`..`、反斜杠、控制字符和现有文件，并验证对应目录可创建、可写且禁止脚本执行。保存失败或字段被拒绝时返回明确错误，不能显示成功。增加修改成功、非法路径拒绝和配置未改变的测试。

### 40. 中危 — 上传历史的事件选择器会删错记录，任意页面按钮还会清空全部历史

**文件**: `app/history.php:24-59`

单条删除处理器虽然绑定到每个 `.Remove` 元素，但回调中使用全局选择器读取 ID：

```javascript
let Remove = $('.Remove').attr("id");
```

jQuery 只返回集合中第一个元素的属性，因此点击第二条及之后记录仍会删除第一条。更严重的是，清空处理器绑定为 `$('button').on('click', ...)`，会覆盖页面中导航折叠、弹窗及未来新增的所有按钮；用户点击任何一个按钮都可能执行 `$.zui.store.clear()`，且没有确认步骤。历史记录仅保存在浏览器本地，清空后无法从服务器恢复。

**修复要求**：单条删除使用 `$(this).attr('id')` 或稳定的 `data-key`，清空操作只绑定专用 ID/类并要求明确确认。用 DOM API 构建卡片并通过事件委托处理动态元素，避免全局选择器。增加多条记录删除第二条、取消清空、导航按钮不影响历史和专用清空按钮的浏览器测试。

### 41. 高危 — 文件管理器按一个文件名校验扩展名，却按另一个文件名落盘，可上传任意扩展名

**文件**: `admin/manager.php:837-864`, `admin/manager.php:874-935`

文件管理器从 `$_FILES['file']['name']` 提取扩展名并与 `FM_UPLOAD_EXTENSION` 比较，却用独立的 `$_REQUEST['fullpath']` 经 `basename()` 后生成最终路径。文件名合法性判断还使用了 `&&`，只有两个名称同时非法才拒绝。除 SVG 外，落盘前没有校验目标名称的扩展名。

攻击者只需在已认证的上传请求中令 multipart 文件名为 `safe.jpg`、`fullpath` 为 `payload.php`，`$isFileAllowed` 就会通过，普通上传和分片合并最终都会写入 `payload.php`。同理可写入 `.user.ini`、HTML 或服务器特有的可执行扩展名。`i/.htaccess` 只对启用 AllowOverride 的 Apache 生效，Nginx、IIS 或错误配置的 Apache 仍可能执行上传内容，形成管理员会话到远程代码执行的升级路径。

**修复要求**：服务端只生成一个规范化目标名称，并用该名称完成合法字符、允许扩展名、SVG 内容和保留文件名检查；客户端字段不能决定未经复核的落盘扩展名。分片首块固定并持久化元数据，后续块必须完全匹配，最终合并后再以 `finfo`/图片解码验证内容并原子改名。无论 Web 服务器类型，都应将存储目录部署在不可执行位置。增加文件名与 `fullpath` 扩展名不一致、双扩展名、点文件、大小写、分片篡改及正常图片测试。

### 42. 高危 — 文件管理器批量操作未清理 `file[]`，可越过图片根目录读写或删除站点文件

**文件**: `admin/manager.php:966-1031`, `admin/manager.php:729-776`

单文件删除会对名称调用 `fm_clean_path()` 并移除斜杠，但批量删除直接执行 `$new_path = $path . '/' . $f`；批量复制/移动同样直接把每个 `$f` 拼到源路径和目标路径。打包功能在图片目录中 `chdir()` 后，将未验证的 `file[]` 原样交给 `ZipArchive`/`PharData`。

因此携带有效管理员 Session 和 CSRF Token 的构造请求可提交 `file[]=../../config/config.php`：批量删除可移除配置或其他站点文件；复制可把配置复制到公开图片目录造成凭据泄露，移动还会破坏源文件；打包可将根目录外的文件收进公开归档。公开反射型 XSS（问题 #32）可在管理员浏览器中读取 Token 并利用该接口，令其从 XSS 升级为配置泄露或站点破坏。

**修复要求**：所有文件操作统一接收经过严格验证的单个相对路径组件或服务器生成的对象 ID，禁止绝对路径、空组件、`.`、`..`、反斜杠、NUL 和符号链接逃逸；在每次读写前使用 `realpath()`/父目录解析验证源与目标均位于规范化 `FM_ROOT_PATH` 下。批量操作应逐项验证，任一非法项时整批拒绝，且禁止归档根外对象。增加删除、复制、移动、打包的 `../`、编码变体、绝对路径及符号链接回归测试。

### 43. 中危 — 文件管理器下载响应的 MIME 和 Range 解析错误，断点续传会在 PHP 8 直接崩溃

**文件**: `admin/manager.php:3038-3094`

下载函数使用单引号调用 `header('Content-Type: $contentType')`，响应会发送字面量 `$contentType`，而不是已计算的 MIME。收到标准请求头 `Range: bytes=100-199` 后，代码仅按 `=` 分割，将字符串 `100-199` 留在 `$range`；错误的 `str_replace($range, '-', $range)` 既替换方向颠倒，也没有保存返回值。随后范围字符串参与算术、拼成格式错误的 `Content-Range`，并直接传给 `fseek()`。

在 PHP 8 中 `fseek($fp, '100-199')` 会抛出 `TypeError`，所以浏览器的断点续传、音视频拖动或下载器的分段请求都会得到半截 206 响应后 500。即使旧版 PHP 将其隐式转换，代码也忽略结束偏移并持续输出到 EOF，`Content-Length` 与实际正文不一致。

**修复要求**：使用真实 `finfo` MIME 并正确插值响应头；按 RFC 7233 严格解析单一字节范围，支持 `start-end`、`start-` 和 `-suffix`，拒绝多范围及越界请求并返回 416。以整数保存起止位置，只输出声明长度，正确设置 200/206、`Content-Range` 和 `Content-Length`，关闭输出缓冲并处理客户端中断。增加无 Range、三种合法范围、非法/多范围、零字节和 PHP 8 回归测试。

### 44. 高危 — 版本检查禁用 TLS 验证并原样渲染远端内容，可向管理员注入持久化脚本

**文件**: `app/class.version.php:50-78`, `app/function.php:1119-1135`, `admin/admin.inc.php:1015-1056`

版本客户端请求 GitHub API 时明确设置 `CURLOPT_SSL_VERIFYPEER=false` 和 `CURLOPT_SSL_VERIFYHOST=false`，随后把响应缓存到 `admin/logs/version/version.json` 十天。后台把远端 release 的 `name`、`body`、`created_at` 和 `zipball_url` 直接 `echo` 到 `data-custom` 属性、`<pre>` 正文或 `href`，没有 HTML/属性/URL 上下文转义。

能够劫持服务器出站网络或伪造证书的攻击者可返回含引号和标签的 release JSON，在管理员打开设置页时执行持久化 JavaScript，并借其 Session/CSRF Token 修改配置或利用文件管理器问题。即使只篡改 `zipball_url`，管理员也会被引导下载攻击者控制的伪造升级包。

**修复要求**：启用系统 CA 校验（`VERIFYHOST=2`、`VERIFYPEER=true`），限制协议/重定向、设置合理超时并验证 HTTP 200、JSON 类型和字段长度；失败时保留最后一个已验证缓存，不能用错误响应覆盖。所有文本按输出上下文使用 `htmlspecialchars()`，JavaScript 数据用 `json_encode()`，下载 URL只允许 GitHub 官方 HTTPS 主机。可进一步校验签名/发布摘要。增加无效证书、恶意 release 字段、异常 JSON、超长响应和缓存回退测试。

### 45. 中危 — 默认登录背景通过明文 HTTP 无界抓取并直接写缓存，可被劫持导致内存/磁盘耗尽

**文件**: `config/config.php:110`, `app/bing.php:13-54`

默认 `login_bg` 指向公开的 `app/bing.php`。每天缓存不存在时，该入口通过 `http://cn.bing.com` 获取 JSON 和图片；`readfile()` 的全部输出先进入 `ob_start()` 缓冲，再一次性取出并写盘。代码没有 TLS、连接/总超时、响应状态、JSON 结构、Content-Type、图片解码、字节上限或尺寸上限检查。

服务器出站链路上的攻击者或异常上游可以返回任意大响应，每次新日期的登录页访问都可能耗尽 PHP 内存、执行时间或图片分区空间。并发首访还会同时以 `w+` 截断同一文件，没有锁和原子改名，管理员最终可能得到损坏或空白背景；失败后仍尝试读取不存在的缓存并把 200 `image/jpeg` 返回给浏览器。

**修复要求**：只使用证书验证开启的 HTTPS；以 cURL 流式下载到站点外临时文件，设置连接/总超时、最大响应字节和低速限制，校验 200、最终主机、MIME、`getimagesize()`/解码结果及像素上限后原子改名。用互斥锁合并同日并发刷新，失败时返回受版本控制的本地背景和正确状态码。增加 MITM/无效证书、超大响应、坏 JSON、非图片、超时和并发刷新测试。

### 46. 低危 — 退出登录由无 CSRF 防护的 GET 请求改变 Session 状态

**文件**: `admin/index.php:10-40`, `app/header.php:56-59`

导航将退出链接实现为 `GET /admin/index.php?login=logout`，入口只要看到 `login` 参数就调用 `auth_logout()`，没有方法限制或 CSRF Token。`SameSite=Lax` Cookie 会在跨站顶层 GET 导航中发送，因此攻击者可通过外部链接、跳转或钓鱼页反复强制管理员和游客上传账号退出，造成操作中断；该问题不依赖也不要求修改项目说明中特别保留的赋值判断写法。

**修复要求**：退出改为仅接受 POST 的小表单，提交并验证当前 Session 的 CSRF Token；成功后销毁服务端 Session、删除 Cookie，再用 303 跳转。GET 入口只展示确认页或返回 405，不改变认证状态。增加跨站 GET 不退出、缺失/错误 Token 拒绝、合法 POST 退出及旧 Session 失效测试。

### 47. 中危 — 上传历史将原始文件名和链接直接拼接为 HTML，可由恶意图片文件名触发存储型 DOM XSS

**文件**: `app/upload.php:226-237`, `api/index.php:179-192`, `index.php:181-199`, `app/history.php:22-31`

上传响应把客户端原始文件名主体放入 `srcName`，主页将整个响应保存到同源 `localStorage`。历史页遍历记录时用长字符串调用 `$('.listNum').append(...)`，把 `srcName`、`url`、`thumb` 和 `del` 未经任何编码地放入元素正文及 `id`、`href`、`src`、`data-*` 属性。

Linux 和浏览器 File API 允许文件名包含引号、尖括号等字符。攻击者可诱导已登录管理员上传一个内容正常但名称类似 `"><img src=x onerror=...>.png` 的图片；上传本身成功，随后管理员打开历史页即会解析攻击载荷。脚本可读取页面中的 CSRF Token并执行后台操作。历史记录持久化在浏览器中，刷新后仍会触发，直到清除存储。

**修复要求**：不要拼接 HTML；用 `document.createElement()`/jQuery DOM API 创建节点，正文只用 `textContent`，属性通过 `setAttribute` 设置。对 URL 先以 `URL` 解析，只允许预期的 `http:`/`https:` 或站内路径，删除链接还应限制到本站 `app/del.php`。服务端可另外返回展示用安全文件名，但不能替代客户端上下文安全。增加含单双引号、标签、事件属性、`javascript:` URL 的历史记录及恶意原始文件名端到端测试。

### 48. 中危 — 网页和 API 上传端从未执行管理员配置的扩展名白名单

**文件**: `app/function.php:676-681`, `index.php:143-153`, `app/upload.php:97-103`, `api/index.php:78-84`, `app/class.upload.php:1909-1990`

`$config['extensions']` 只通过 `getExtensions()` 传给浏览器上传控件，服务端的两个上传入口都不读取它。开启 `allowed` 时，处理器仅设置 `$handle->allowed = array('image/*')`，因此任何底层库识别的图片类型和任意非危险扩展名都可绕过管理员选择；攻击者也可完全跳过前端直接调用接口。关闭“图床模式”时则使用上传类内置的广泛文档/压缩包 MIME 列表，而不是后台所称的“任意指定格式”，配置仍然不生效。

这会使格式治理、Web 服务器响应规则、压缩/缩略图兼容假设和运营策略失真。例如管理员只允许 `jpg,png` 时，API 仍可保存 WebP/AVIF 或带未知扩展名的有效图片；非图床模式也可能接收配置列表外的 PDF、Office 或归档文件。

**修复要求**：在实例化后、任何落盘前，从 `sanitize_upload_extensions()` 得到唯一服务端白名单，并同时验证规范化目标扩展名、可信 `finfo` MIME 和实际图片解码结果；图床模式还必须要求 `file_is_image`。若确需通用文件模式，应提供独立、明确的 MIME/扩展名配置及不可执行存储策略，不能复用第三方默认列表。网页、API、分片完成和文件管理器应调用同一验证器。增加绕过前端、配置外图片、未知扩展名、双扩展名、伪造 MIME 与合法格式测试。

### 49. 高危 — 分片上传不限制未完成任务数量，公开访问者可无界占满系统临时盘

**文件**: `index.php:1-4`, `app/upload.php:64-90`, `app/function.php:2593-2649`, `app/function.php:2660-2686`

开启 `chunks` 后，任何主页访问者都会获得 Session 绑定的 `_upload_id`。`chunk()` 只限制单个任务的块数、单块大小和最终总大小；任务目录的 HMAC 又包含攻击者可控文件名，所以同一 Session/IP 可用不同名称创建无限多个 bucket，并只上传第一块使其永久保持“不完整”。游客每日配额在完整上传成功后才增加，对这些请求无效，网页入口也没有类似 API 的请求速率限制。

每个未完成任务可占用一个接近 `$config['chunks']` 的分片。清理逻辑仅在新的分片请求到来时扫描，并且只删除 24 小时前的目录；攻击者可持续刷新或快速创建任务，在单日内耗尽 `sys_get_temp_dir()` 所在分区，影响全站上传、Session 或同机其他服务。

**修复要求**：为 Session、可信客户端 IP 和全局设置同时进行的任务数、临时总字节数和创建速率上限；创建任务时原子预留配额，每块写入前后在锁内核算，完成/取消/过期时释放。使用不可由普通请求拖延的定时清理，并设置远短于 24 小时的空闲超时。配额元数据不可只由目录遍历推断。增加大量唯一文件名、只传首块、并发配额、重复块、取消/超时释放和磁盘不足测试。

### 50. 中危 — 外部内容审核关闭 TLS 证书验证，可泄露 API Key并篡改审核结论

**文件**: `app/function.php:1202-1223`, `app/function.php:1233-1264`

ModerateContent 请求使用 HTTPS，却显式设置 `CURLOPT_SSL_VERIFYPEER=false` 和 `CURLOPT_SSL_VERIFYHOST=false`，审核 API Key 还直接位于 URL 查询串。NSFWJS 客户端对管理员配置的 HTTPS 地址也关闭相同验证，并允许跟随重定向。两处都没有验证最终 URL、HTTP 状态、响应 MIME 或 JSON schema。

能够观察或劫持服务器出站网络的攻击者可获取 ModerateContent Key并伪造 `rating_index`/`predictions`，让违规图片保持公开，或将任意正常图片判为违规并移动。异常/截断响应还会触发未定义数组访问，审核流程静默失效。若 NSFWJS 地址被重定向，上传图片内容也可能被发送到非预期主机。

**修复要求**：所有 HTTPS 调用启用系统 CA 和主机名验证；将密钥放入供应商支持的认证头（若 API 允许），避免日志/代理记录查询串。固定 ModerateContent 主机，限制 NSFWJS 初始及重定向协议/主机，校验 2xx、JSON Content-Type、字段类型和数值范围。明确采用 fail-open 或 fail-closed 策略并记录可审计错误。增加无效证书、跨主机重定向、伪造/缺字段响应、超时及密钥不出现在日志中的测试。

### 51. 中危 — 用户删除能力链接以 GET 立即改变图片状态，容易被预览器或扫描器误触发

**文件**: `app/upload.php:205-209`, `api/index.php:159-163`, `app/del.php:23-71`

上传响应返回带长期能力 Token 的 `/app/del.php?hash=...`。该 URL 被任何客户端 GET 时都会立即移动或永久删除图片，没有确认页、请求方法限制、一次性 nonce 或交互证明。聊天软件预览、邮件安全扫描、搜索/归档机器人、浏览器链接预取以及用户误开链接都可能在不知情时消费删除能力。

Token 位于查询串，还会出现在反向代理访问日志、浏览器历史、复制记录及潜在 Referer 中；任何获得它的人都能随时删除对应图片。加密和 MAC只能防伪造，不能防泄露与非人为 GET。开启回收站会降低永久丢失概率，但仍使公开 URL立刻失效；关闭回收站时则直接不可逆删除。

**修复要求**：GET 只显示不包含第三方资源的确认页，并设置 `Referrer-Policy: no-referrer`、`Cache-Control: no-store`；实际删除必须 POST，提交能力 Token和同页生成的短期一次性 nonce，成功后令 Token失效或轮换。API 客户端可使用明确的 `DELETE`/POST 端点并放在请求体或授权头。为爬虫 GET、预取、重复提交、过期 nonce、泄漏 Token和正常确认删除增加测试。

### 52. 中危 — 广场批量删除/回收无逐项结果，失败和空操作都会向管理员显示成功

**文件**: `app/del.php:86-114`, `app/list.php:253-346`

批量删除循环忽略 `getDel()` 和 `any_upload()` 的结果，对非法路径、文件不存在、权限错误及 FTP 失败都固定返回 `code: 200`、`type: success`。批量回收同样忽略 `checkImg()` 结果，完成后不输出任何结构化响应。前端回收请求没有回调，发送后立即提示成功；删除回调只搜索响应中是否含 `success`，而服务端固定包含该字符串。

管理员因此无法知道哪些图片实际处理成功。部分本地删除失败时仍可能调用 FTP 删除，或反过来远端失败而本地已删除，造成两端分叉；空选择也会提示完成。自动刷新进一步掩盖错误，批量破坏性操作没有可审计的失败清单。

**修复要求**：先验证请求数组非空并设合理数量上限；逐项记录规范化路径、本地结果、FTP 结果及错误码，返回统一 JSON和合适 HTTP 状态。明确批量操作采用全有或全无，还是可重试的部分成功语义；前端必须等待响应、解析 `code`/逐项结果后再提示和刷新。增加全成功、全失败、部分失败、空数组、非法项、FTP 分叉和网络错误测试。

### 53. 高危 — 删除链接和源图保护共用管理员密码哈希派生密钥，正常改密会使全部既有链接失效

**文件**: `app/function.php:883-938`, `app/upload.php:199-207`, `api/index.php:155-161`, `admin/admin.inc.php:193-235`

`urlHash()` 总是以 `$config['password']` 作为密钥材料；即便传入所谓独立的 `hide_key`，实现也是把它附加到密码哈希后再派生。管理员改密会生成带新随机盐的新 password hash，所以即使明文密码不变，配置中的字符串也会变化。所有历史 Token 都没有 key ID 或旧密钥回退。

因此每次合法改密都会使此前返回给用户/API 的删除链接无法解密；开启 `hide` 时，所有已发布的源图保护 URL也会立即统一返回 404，造成全量图片链接中断。删除链接失效还意味着用户失去自行删除旧图片的能力。凭据轮换与内容地址生命周期被错误耦合，迫使管理员在安全改密和链接可用性之间选择。

**修复要求**：安装时生成独立的高熵应用密钥和 key ID，分别按用途派生删除/隐藏密钥，不得依赖用户名、密码或密码哈希。Token封装版本、用途、key ID、签发时间和可选过期时间；支持受控密钥环轮换，先签发新密钥、保留只解密旧密钥的迁移窗口。应用密钥应位于 Web 根外或环境/密钥管理系统中。增加改密前后旧链接仍有效、用途隔离、密钥轮换、旧 key 退役和错误 key ID测试。

### 54. 高危 — 回收站用下划线扁平化路径且无法无损反解，常见文件名不能恢复并会发生碰撞

**文件**: `app/function.php:1361-1369`, `app/function.php:1387-1425`

图片移入 `recycle/` 或 `suspic/` 时，代码把相对路径中的 `/` 和 `\\` 全部替换为 `_`；恢复时却执行 `str_replace('_', '/', $name)`。该变换不是双射，无法区分原目录分隔符和文件名本来就有的下划线。

例如 `2026/08/30/my_photo.jpg` 被保存为 `2026_08_30_my_photo.jpg`，恢复目标却变成 `2026/08/30/my/photo.jpg`，其父目录通常不存在，恢复直接失败。`a/b_c.jpg` 与 `a_b/c.jpg` 还会映射到同一个回收文件名，后到图片因目标已存在而无法进入回收站。项目的日期式/缩略图命名本身大量使用下划线，所以这不是罕见边界情况；关闭回收站前若依赖其可恢复语义，会造成实际数据丢失。

**修复要求**：回收文件使用随机对象 ID或无歧义编码（例如 base64url/长度前缀），另以受保护的结构化元数据保存原规范相对路径、时间和校验和；恢复时解析元数据并再次经过存储边界验证，按需安全创建原父目录且明确处理同名冲突。提供旧扁平名称的迁移/人工映射工具。增加含下划线、多层目录、中文、同名碰撞、父目录缺失、重复恢复及旧格式迁移测试。

### 55. 高危 — FTP 上传失败后仍无条件删除本地文件，接口已报告成功却可能丢失唯一副本

**文件**: `app/upload.php:226-264`, `api/index.php:179-222`, `app/function.php:2506-2583`

`any_upload()` 捕获 FTP 连接、建目录或 `put()` 异常后只输出错误，不保存失败状态，也不提前返回。函数末尾只要 `ftp_complete_del_local` 开启，就无条件执行 `@unlink($localFile)`。远端写入是否完成、传输字节是否一致、连接是否已建立均不影响本地删除。

两个上传入口在调用 FTP 前已输出成功 JSON，并可能执行 `fastcgi_finish_request()`；因此 FTP 认证错误、网络中断、配额满、权限不足或部分传输时，客户端仍拿到成功链接，后台却删除本地原图，远端也没有完整副本。异常被 `@` 和输出时序隐藏，系统没有可靠重试队列或失败审计。该删除逻辑还不区分 `upload`、`delete`、`rename` 模式，进一步增加错误目标被清理的风险。

**修复要求**：FTP 操作返回明确的成功对象/异常，只有远端临时文件上传完成、大小（最好加哈希）校验一致并原子改名后，才允许删除规范化存储根内的本地原图；删除本地应只属于成功的 upload 模式。上传响应必须反映选定的一致性策略，或先进入持久化重试队列并保持本地副本。记录结构化错误，禁止向已结束的 JSON 响应追加文本。增加连接/登录/建目录/中途传输/校验/改名失败、成功清理和进程重启重试测试。

### 56. 中危 — TinyPNG 压缩直接以远端流覆盖原图，网络或写入失败会留下截断文件却仍报告成功

**文件**: `app/compress/TinyImg/TinyImg.php:30-61`, `app/compress/function.compress.php:37-46`, `app/compressing.php:37-46`

TinyPNG 模式把输入与输出设为同一路径。取得 API 的 `Location` 后，代码直接执行 `file_put_contents($output, fopen($location, 'rb'))`；目标文件会在远端流完整下载和验证之前被截断。实现没有连接/读取超时、HTTP 状态检查、Location 协议/主机限制、响应大小、图片解码或写入字节数验证，也没有备份和原子改名。

远端下载中断、磁盘写满、证书/代理异常或返回非图片时，原图可能变成零字节、部分数据或错误正文。`compressImg()` 不返回结果，文件夹循环和页面也不累计失败，最终无条件显示“压缩完毕”；管理员无法识别或恢复已损坏文件。PHP 8 下 `fopen()` 返回 false 后传给 `file_put_contents()` 还可能抛出类型错误并中止整批任务。

**修复要求**：将结果限时、限量流式写入同目录的随机临时文件，验证 2xx、最终 HTTPS 目标、字节数、`finfo`、图片完整解码及合理尺寸后 `fsync` 并原子替换；失败时保留原文件。每项返回结构化状态，批处理汇总成功/失败并提供可重试清单。增加中途断流、HTTP 错误、恶意 Location、非图片、磁盘满、PHP 8 false stream和成功原子替换测试。

### 57. 高危 — 内容审核发生在公开发布和成功响应之后，违规图片可在判定前被访问并永久缓存

**文件**: `app/upload.php:166-202`, `app/upload.php:226-258`, `api/index.php:125-157`, `api/index.php:179-216`

两个上传入口先把文件写入 Web 可直接访问的存储目录，生成并返回公开 URL，然后调用可立即结束响应的 `fastcgi_finish_request()`；`process_checkImg()` 在此之后才请求外部审核并将违规文件移入 `suspic/`。ModerateContent 最长可等待 30 秒，后台进程排队或远端异常时窗口更长。

上传者拿到成功响应后可立即并发读取原图，也可让 CDN、代理、搜索机器人或第三方聊天预览缓存它。即使审核随后正确判违规并移动本地文件，已经分发的副本不会被撤回；若外部服务失败，当前流程没有明确阻止发布。因而“开启鉴黄”无法提供发布前门禁，合规和内容安全承诺失效。

**修复要求**：新上传先写入不可公开访问、不可执行的隔离目录，以稳定对象 ID进入明确状态机；审核通过后再执行后续处理并原子发布，拒绝/失败按配置进入人工复核或删除。客户端在同步模式下等待最终结论，异步模式只收到不可猜的状态 ID并轮询，绝不能提前收到可访问原图 URL。发布与 CDN 缓存失效需有一致策略。增加慢审核、并发读取、审核超时/错误、拒绝、人工复核和发布原子性测试。

### 58. 中危 — 水印、压缩和远端发布均在成功响应后修改同一 URL，客户端会看到未处理、404 或陈旧缓存

**文件**: `app/upload.php:188-264`, `api/index.php:146-222`

上传接口先返回 `url`/`thumb` 和 `code: 200`，之后才依次写日志、加水印、原地压缩并上传 FTP。客户端或 CDN可在水印前读取并缓存未加标识的原图；压缩随后在相同 URL下改变字节、尺寸和校验值，却没有缓存版本或失效通知。若 `imgurl` 指向 FTP 对外域名，返回的 URL在远端传输完成前会短暂 404。

任一后处理失败都发生在响应完成后，无法把成功改为失败；无 FastCGI 的环境还可能把 FTP 错误文本追加到 JSON使响应无法解析。上传日志又可能在水印/压缩前后记录不同对象（问题 #34进一步把路径指向缩略图），导致用户链接、实际文件、缓存和审计信息互不一致。

**修复要求**：定义单一发布事务：在非公开临时路径完成水印、压缩、校验和必要远端复制后，再一次性发布最终不可变对象并返回 URL。内容变化使用内容寻址或版本化 URL；远端发布需确认可读后切换。若采用异步任务，返回 `202 + job_id`，客户端只在状态为 ready 后取得最终 URL。所有处理器返回结构化错误且可重试。增加处理中的并发 GET、CDN 缓存、FTP 延迟、每个后处理失败和最终字节/日志一致性测试。

### 59. 高危 — 公开缩略图允许超大画布和无限缓存键，可用单请求耗尽内存或批量占满缓存盘

**文件**: `app/thumb.php:49-69`, `app/TimThumb.php:242-268`, `app/TimThumb.php:320-336`, `app/TimThumb.php:608-626`

`thumb.php` 将 `MAX_WIDTH`/`MAX_HEIGHT` 都设为 10240，并公开接受 `w`、`h`、质量、滤镜等参数。一个 10240×10240 真彩画布本身约需 400 MiB，尚未计源图和中间画布，已经超过 TimThumb 设置的 256 MiB；匿名请求即可造成 PHP OOM或长时间 CPU占用。

服务端缓存文件名使用完整 `$_SERVER['QUERY_STRING']` 计算 MD5。任意无关参数、参数顺序或等价值都会生成新缓存，即使输出像素相同；攻击者可对一张已知公开图片不断改变垃圾参数，绕过缓存复用并在清理周期内创建无限变体。缓存清理仍需 `glob()` 遍历全部文件，文件越多每个过期清理请求越昂贵，形成 CPU、内存和磁盘复合 DoS。

**修复要求**：把输出尺寸限制在业务需要的较小范围，并按像素预算在解码前拒绝 `source_pixels + destination_pixels + overhead` 超过安全内存；限制质量/滤镜组合。缓存键只由验证、规范化后的有效参数白名单和源文件版本构成，忽略未知参数；为每张源图、IP及全局设置变体数/生成速率/缓存总量配额，使用可预测成本的淘汰机制。增加最大尺寸、像素预算、未知参数、等价参数、变体洪泛、并发同键和缓存淘汰测试。

### 60. 中危 — 公开统计在请求线程递归全盘扫描且无缓存锁，并发过期请求可放大为磁盘 I/O DoS

**文件**: `api/public.php:17-113`, `app/total_files.php:19-96`, `app/chart.php:35-99`

启用公开数据 API 后，缓存缺失或过期的普通匿名请求会直接执行统计。总量缓存递归遍历整个图片根计算文件/目录数，又再次递归计算空间；月统计对最近 30 天分别执行文件数与目录大小扫描，共至少 60 次目录遍历。所有工作都在 HTTP 请求线程内完成，没有时间、节点数或 I/O预算。

缓存刷新没有互斥、single-flight 或临时文件原子替换。攻击者可在过期时并发请求，使多个进程同时扫描同一目录并以 `file_put_contents()` 覆盖同一 JSON；其他请求可能读取半写文件，`json_decode()` 得到 null后继续索引并产生错误。符号链接目录也未统一排除，可能扩大扫描边界或形成递归环。图片规模越大，公开 API越容易拖垮 PHP worker和存储。

**修复要求**：把统计迁移到定时/队列任务，HTTP 端只读取最后一个完整快照；刷新使用进程间锁、single-flight、临时文件 + `fsync` + 原子改名，并保留上次成功版本。遍历固定规范存储根，拒绝符号链接，设置最大节点/耗时且使用迭代器避免深递归。响应缓存失效时返回陈旧但有效数据或 503，不同步重算。增加大目录、并发过期、半写/坏缓存、符号链接环、任务失败回退和性能预算测试。

### 61. 中危 — 安装器错误要求 PHP 文件为 `0755` 并建议整站归 Web 用户所有，诱导高风险权限配置

**文件**: `install/index.php:14-29`, `install/index.php:92-123`

Linux 环境检测只有在 `app/upload.php` 权限字符串恰好为 `755` 时才通过，否则阻止进入下一步。Apache module、PHP-FPM 和 CGI Web SAPI通常只需读取 PHP源码，安全且常见的 `0644` 不需要执行位；文件 ACL、组读权限等有效部署也被错误判失败。

失败页建议直接执行 `chmod 755 -R APP_ROOT` 和 `chown -R www:www APP_ROOT`。这会给所有普通数据文件增加执行位，并让 Web 服务账号拥有配置、PHP源码、安装器、静态资源乃至仓库元数据的写权限。任一文件写入、后台 XSS或受限文件管理漏洞因此更容易升级为持久化 PHP代码执行，也使攻击者可篡改安全配置和审计代码。

**修复要求**：按实际能力检查：代码/配置只需 Web 进程可读且不应可写，只有 `config/` 安装阶段、图片存储、必要日志/缓存目录应最小化可写；用测试写入并立即删除随机临时文件验证目录能力，不比较固定 mode。文档提供按用户/组和部署模型区分的最小权限示例，安装完成后收紧配置并移除安装写权限。增加 `0644` 代码 + 可写数据目录、只读代码、ACL、错误属主和安装后权限审计测试。

### 63. 高危 — `storage_path` 与 API Token ID未经路径约束，可让上传落到图片存储根目录之外

**文件**: `admin/admin.inc.php:39-75`, `admin/admin.inc.php:93-102`, `app/function.php:558-583`, `app/upload.php:151-167`, `api/index.php:118-125`

后台允许保存任意 `storage_path` 字符串，服务端仅把它传给 `date()`，再与 `$config['path']` 拼接；PHP 日期格式可用反斜杠转义字面字符，因此可构造包含 `../` 的输出。`config_path()` 不做段规范化或真实路径边界检查。API Token 的 `add_token_id` 也没有类型、长度或字符验证，开启 Token 分目录后，该 ID直接与日期路径拼接。

配置或 Token ID为穿越值时，`$handle->process(APP_ROOT . $Img_path)` 会在图片根外创建目录并保存上传内容，例如进入 `public/`、`config/` 或应用其他可写位置。返回 URL、删除、隐藏、统计和 FTP 同步仍假定对象位于 `$config['path']`，导致文件公开策略绕过、孤儿文件和无法管理/删除。问题 #32 的管理员 XSS还可代替用户提交这些设置，扩大到持续的任意站内目录写入。

**修复要求**：存储布局不要接受任意 PHP `date()` 格式；提供固定枚举或解析后只允许安全日期占位符和 `/`，拒绝 `.`,`..`、反斜杠、空/绝对段及控制字符。Token ID限定为短整数或安全 slug，并在保存和使用时双重验证。所有新文件目标都通过统一 resolver：规范化父目录、确认其真实路径在存储根内、拒绝符号链接后再创建。增加转义 date 格式、Token ID穿越、绝对路径、符号链接父目录和正常布局测试。

### 64. 严重 — 配置序列化器把纯数字字符串写成 PHP 数字字面量，特定合法设置值会使全站语法崩溃

**文件**: `app/function.php:1627-1649`, `admin/admin.inc.php:21-108`, `admin/admin.inc.php:153-184`

`arrayeval()` 不按值的真实 PHP 类型序列化，而是把长度不超过 12 的纯数字字符串一律不加引号写入配置源码。后台多个本应保持字符串语义的字段都经过该函数，例如站点标题、水印文字、FTP 用户名/密码、隐藏密钥、存储格式及第三方 Key。输入 `008` 时生成类似 `'title'=>008` 的源码；PHP 会把前导零解释为八进制，而其中出现 `8` 或 `9` 会直接触发 `Invalid numeric literal` 解析错误。即使所有数字均小于 8，`00123` 也会被读取成八进制整数并永久丢失前导零。

管理员只需在正常设置表单保存一个这样的值，`config/config.php` 就可能无法再被任何入口 `require`，首页和管理后台一起白屏，连再次进入后台修复都做不到。FTP 密码、API相关标识和密钥被静默改变还会造成认证失败或安全材料退化。当前写入路径在覆盖正式配置前不生成候选文件并执行 `php -l`，也不会因序列化语义变化而拒绝保存。

**修复要求**：删除按字符串内容猜测类型的逻辑，使用 `var_export()` 或等价的类型保持序列化器；只有配置 schema 明确定义为整数的字段，才在输入验证阶段完成有范围检查的转换。先把完整候选配置写入同目录随机临时文件，执行语法/结构校验并重新加载核对关键类型，成功后再原子替换正式文件，失败时保留旧配置并向管理员报告。增加 `008`、`09`、`00123`、12/13 位数字字符串、纯数字密码/密钥、真实整数、布尔值和嵌套数组往返测试。

### 65. 高危 — 配置文件在加锁前被原地截断且忽略短写，保存异常会让所有请求加载残缺 PHP

**文件**: `app/function.php:1606-1617`, `app/function.php:1659-1668`, `admin/admin.inc.php:39-179`

`writefile()` 先以 `w` 模式打开正式目标，再调用 `flock()`；文件在锁成功前已经被截断。它不检查加锁返回值、`fwrite()` 是否写完全部字节、刷新/关闭是否成功，也没有同目录临时文件和原子改名。另一个 PHP worker可在写入期间 `require` 到空白或半截 `config.php`，磁盘满、配额、I/O错误或进程中止则会永久留下残缺源码。锁只能串行化后续写操作，不能保护无锁读者，也不能撤销打开时的截断。

普通设置、Token增删及访客账号停用/删除等多数后台分支完全忽略 `cache_write()` 返回值并固定显示“成功”。配置文件是每个请求的启动依赖，因此一次失败不只丢失本次变更，还可能让首页、API和管理后台全部解析失败，必须登录服务器才能恢复。并发管理请求还基于各自读取的旧数组做整文件覆盖，后一写入会静默丢掉前一请求的修改。

**修复要求**：在目标同目录创建权限受控的随机临时文件，循环写完全部字节并检查每次返回，`fflush`/可用时 `fsync` 后校验生成内容，再用原子 `rename` 发布；使用覆盖整个读取-合并-发布事务的独立锁文件，读端只会看到旧版或完整新版。所有调用者必须检查结果，失败时返回 5xx且不能显示成功；并为配置保留权限严格、可恢复的最近有效备份。增加并发读取/保存、两个并发字段更新、短写、磁盘满、锁失败、进程中止和故障后旧配置仍可加载测试。

### 66. 中危 — 上传计数和日志每次写入都重置全局 OPcache，可被上传流量持续放大为性能 DoS

**文件**: `app/function.php:1606-1617`, `app/function.php:2200-2250`, `app/function.php:2278-2303`, `app/upload.php:250-256`, `api/index.php:208-214`

所有 PHP 数组文件都通过 `cache_write()` 保存，而该函数写完任意目标后无条件调用 `opcache_reset()`，清空整个 PHP worker池的共享字节码缓存。开启上传日志或游客每日计数后，每个成功网页/API上传分别触发一次重置；两项同时开启时单次上传会连续重置两次。随后站点和同一 FPM池中的其他应用必须重新编译 PHP源码，缓存尚未变热又可被下一次上传清空。

日志和计数本是用来限制/审计滥用的功能，开启后反而让普通上传流量获得稳定的 CPU和延迟放大器。API虽有按 Token速率限制，网页入口在默认公开模式下没有请求速率限制；攻击者还可分散 IP或账号。错误被调用处的 `@` 抑制，管理员只能看到整体吞吐下降，而无法从应用结果判断根因。

**修复要求**：配置发布与运行日志存储使用不同原语；优先把高频计数/日志写成非可执行数据格式或专用原子计数存储。若仍需加载 PHP数据文件，只对成功原子替换的目标调用 `opcache_invalidate($path, true)`，不要重置全局缓存，并合并单请求内的重复失效。增加 OPcache 开启时的连续上传、两开关组合、跨应用缓存保持、目标文件更新可见性和吞吐基准测试。

### 67. 高危 — 默认匿名网页上传没有服务端速率、累计字节或磁盘水位限制，可直接写满服务器分区

**文件**: `config/config.php:29-36`, `config/config.php:94`, `index.php:118-157`, `app/upload.php:10-76`, `app/upload.php:97-167`

默认配置 `mustLogin=0`、`ip_upload_counts=0`，任何访问者都能调用网页上传端。单文件 10 MiB和前端单次 30 个文件不是资源配额：`maxUploadFiles` 仅传给 JavaScript 控件，直接 HTTP请求一次一个文件即可无限重复；服务端没有每 IP/Session请求速率、每小时字节、全局存储容量、用户配额或最低剩余磁盘阈值。`sign` 只是客户端时间戳且可自行生成，不构成认证或限流。

攻击者无需 API Token即可持续上传有效图片，直到图片分区、inode或日志所在分区耗尽。随后正常上传、配置保存、Session、统计缓存甚至同机服务都会失败；结合后处理的缩略图、审核、压缩和 FTP，还可同时消耗 CPU、内存及出站带宽。可选的每日张数按请求完成后写入且已有并发竞态（第 35 项），即使开启也不按字节或全局容量保护存储。

**修复要求**：提供默认启用的服务端分层配额：可信客户端 IP/Session/账号的令牌桶和小时/日字节数，全局并发上传数、存储总量及 inode预算，并在低于保留磁盘水位时提前拒绝新上传。配额检查与落盘预留必须原子，最终文件、分片、缩略图及日志都计入并在删除后释放；反向代理限流只能作为补充。公开运营模式应在安装时明确选择并展示容量风险。增加绕过前端、持续小/大文件、并发预留、分布式来源、磁盘低水位、inode耗尽和删除释放配额测试。

### 68. 高危 — 安装包自带公开且有效到 2050 年的固定 API Token，开启 API 即暴露上传权限

**文件**: `config/api_key.php:1-15`, `install/contorl.php:44-66`, `docs/API.md:27-40`, `docs/使用ShareX上传.md:17`, `docs/使用PicGo上传.md:13`

版本库跟踪的 `config/api_key.php` 预置 Token `1c17b11693cb5ec63859b091c5b9c1b2`，其 `expired=2544411528`，约到 2050 年才失效；多份公开文档又直接展示同一值。安装程序只更新 `config.php` 的域名、管理员账号和密码，不重建、清空或轮换 `api_key.php`。因此每个从发行包正常安装的新实例都继承相同长期 Bearer凭据。

API默认关闭只能延迟暴露。管理员在后台开启 API后，如果没有注意并删除这个看似示例的现成 Token，攻击者可从公开仓库/文档取得它并调用 `/api/index.php` 上传文件、消耗存储和后处理资源；Token ID 还会出现在文件名中，使运营者误以为请求来自自己配置的合法客户端。所有部署共享密钥也使泄漏无法按实例隔离。

**修复要求**：发行版的 Token 列表必须为空；安装事务内生成实例唯一的高熵 Token或要求管理员显式创建，且绝不能把可用秘密写入源码、示例或文档。首次启用 API时若检测到已知旧默认值，应阻止启用并提供一键吊销/轮换迁移；发布安全公告要求现有实例检查访问日志和轮换。增加全新安装 Token为空、升级检测旧值、启用前置检查、轮换后旧 Token失效及文档无真实凭据扫描测试。

### 69. 中危 — 新 API Token 使用 `mt_rand()` 生成，长期 Bearer凭据缺少密码学随机性

**文件**: `app/function.php:1920-1932`, `admin/admin.inc.php:557-569`

后台打开 Token管理页时，`privateToken()` 循环调用 32 次 `mt_rand(65, 122)`，再对结果做 MD5并放入表单。Mersenne Twister是为模拟设计的可预测 PRNG，不适合生成认证秘密；MD5不能把可预测输入变成高熵随机值。项目支持的旧 PHP运行时自动播种质量和同进程状态隔离也不能作为安全承诺，观察足够输出、推测种子或共享状态的攻击者可能缩小后续 Token空间。

Token作为长期 Bearer凭据以明文键保存，持有者可直接上传，服务端没有二次证明。生成实现已经有安装器使用的 `random_bytes()`/OpenSSL安全回退，却没有复用于 API Token；管理员也可提交任意长度、任意格式和空字符串作为 Token，服务端不校验熵或冲突。

**修复要求**：统一使用 `random_bytes(32)` 后 base64url/hex编码生成至少 256 bit秘密，旧运行时仅允许可靠的 `openssl_random_pseudo_bytes()` 且检查 strong结果，安全源不可用时拒绝创建。服务端忽略客户端提交的秘密，只接收标签和期限；保存 Token摘要而非原文，仅在创建时显示一次，并以不可复用 ID管理吊销。验证格式、唯一性和期限范围。增加随机源失败、长度/字符集、碰撞重试、Token摘要验证、只显示一次及旧 Token轮换测试。

### 70. 低危 — 首次环境检查在 GD 缺失时先写永久跳过锁再调用不存在的 `gd_info()`，诊断页反而白屏

**文件**: `index.php:223`, `app/check.php:11-12`, `app/check.php:37-45`, `app/check.php:69-78`

主页首次加载 `check.php` 时，代码在执行任何检测前就创建 `config/EasyIamge.lock`。扩展循环虽然能识别 GD未安装并准备输出提示，但后面仍无条件调用 `gd_info()`。在 PHP 7/8 的缺 GD环境中这会抛出未定义函数致命错误，整个首次主页请求中止，用户看不到已经生成的扩展缺失提示。

锁文件已在崩溃前落盘，后续主页请求直接 `return`，所以诊断不会自动重试；修好 GD后也不会再展示其他首次检查结果，除非运维人员知道并手工删除拼写特殊的锁文件。锁写入失败同样没有检查，却仍在界面宣称已生成。

**修复要求**：检测函数存在/扩展状态后再调用扩展 API，缺失时只输出可用的纯文本诊断；全部检查成功渲染或用户明确确认后，才原子创建锁，并检查写入结果。把锁状态与检测版本/结果关联，运行环境变化或上次中止时允许重试。增加无 GD、无 fileinfo、锁不可写、检查中异常、修复扩展后重试和成功只显示一次测试。

### 71. 中危 — 上传失败仍返回 HTTP 200，官方客户端示例会把认证和校验错误报告成成功

**文件**: `api/index.php:18-47`, `api/index.php:60-90`, `api/index.php:128-205`, `app/function.php:2010-2045`, `app/upload.php:10-108`, `docs/API.md:46-59`

网页和 API上传入口把业务编号放进 JSON的 `code` 字段，却在绝大多数失败分支从未设置 HTTP状态。API关闭、Token不存在/过期、无文件、IP拒绝、SVG/分片无效、尺寸/MIME处理失败等都会以 HTTP 200结束；只有新增的速率限制单独返回 429。部分业务码还借用了 HTTP含义但语义不同，例如“无文件”的 204仍带响应体、“Token错误”的 202看起来像已接受异步任务。

官方 Python示例只判断 `response.status_code == 200` 并在此时打印 `Upload successful`，所以它会把上述所有失败当成功。反向代理、监控、重试器和 SDK也无法用标准状态区分成功、客户端错误和服务端错误；错误响应可能被成功指标统计或缓存，真正的 202分片响应又与伪 202错误混淆。

**修复要求**：定义稳定的 HTTP + 应用错误契约：成功 200/201，未完成分片 202，无请求体文件用 400，未认证/Token无效用 401，权限/IP策略用 403，过大用 413，不支持格式用 415，配额用 429，处理/存储失败用 5xx；JSON保留独立、不可与 HTTP混淆的机器错误标识。所有出口通过一个响应助手设置状态、Content-Type和 `JSON_UNESCAPED_UNICODE`。同步修正文档与客户端按状态及 `result`双重校验，并增加每个失败分支的契约测试。

### 72. 中危 — 后台配置只校验键名和数组形状，所有类型、枚举、范围及 URL约束都可绕过

**文件**: `app/function.php:185-209`, `admin/admin.inc.php:39-75`, `admin/admin.inc.php:436-464`, `admin/admin.inc.php:830-893`, `admin/admin.inc.php:1120-1153`

`filter_config_update()` 只保留白名单键，并检查 POST值与现值是否同为数组；所有标量均作为等价字符串接受。表单上的 `type=number/url/range`、`min/max` 和 `<select>` 只是浏览器提示，直接请求可保存负数/超大 `chunks`、`maxSize`、图片尺寸和缓存周期，任意 `theme`/模式值，无效时区、畸形域名/CDN/审核地址，以及不符合格式的颜色和文件路径。除扩展名和可信代理外没有统一服务端 schema。

这些值会直接进入内存/像素预算、循环和网络超时、日期、文件路径及 HTML/JavaScript输出。结果从配置悄然失效、错误链接和持续 Notice，到超大图片后处理消耗 512 MiB worker、构造任意资源地址或持久化破坏页面结构不等。管理员页面看似给出了安全边界，实际接口完全不执行；第 32 项的管理员上下文 XSS还可自动提交这些畸形值形成持久化影响。

**修复要求**：建立唯一配置 schema，为每个键声明精确类型、必选/可空、长度、整数范围、枚举、URL协议/主机策略、路径策略及跨字段约束；提交时拒绝整个无效事务并返回逐字段错误，不能悄悄转换。发布前用候选配置运行 schema校验，启动时对旧配置采用安全默认并发出告警。前端约束从同一 schema生成或保持一致。增加绕过前端的边界值、负数/溢出、非法枚举、坏 URL/时区、数组污染及跨字段不一致测试。

### 73. 中危 — 登录限流允许匿名攻击者用 5 次错误密码全局锁死已知管理员账号

**文件**: `install/install.php:76-87`, `admin/index.php:43-109`, `app/function.php:2396-2471`

每次登录失败同时写入“客户端 IP”和“账号”两个桶；只要用户名等于真实管理员或访客名，就使用不含来源地址的全局 `account:<name>` 桶。任意一个桶在 15 分钟内达到 5 次，后续正确密码也在认证前被拒绝。安装表单默认并公开建议管理员名 `admin`，登录错误消息又区分“密码错误”“账号不存在”和“账号过期”，真实账号很容易确定。

远程攻击者从单个 IP连续提交 5 次错误密码即可让管理员在所有网络位置都无法登录 15 分钟；每次窗口临近结束继续失败即可无限续锁。受影响管理员无法进入后台删除恶意图片、调整配额或查看日志，攻击者可同时利用公开上传扩大影响。按 IP桶不足以缓解，因为造成全局拒绝的是共享账号桶。

**修复要求**：不要用少量失败触发硬性全局账号锁。IP/网段桶继续限速；账号维度采用指数延迟、风险评分或仅对攻击来源生效的软限制，并允许正确凭据配合第二因素/一次性恢复码通过。响应和耗时不得泄露账号是否存在，记录告警但避免日志放大；为管理员提供站外解锁/紧急恢复机制。增加单 IP错误后异地正确登录、分布式猜测、持续续锁、未知/真实账号等价响应和恢复流程测试。

### 74. 中危 — SVG上传虽已禁止，缩略图端仍同源内联执行历史 SVG，升级后存量 XSS没有清理

**文件**: `app/thumb.php:82-115`, `app/base.php:25-29`, `app/function.php:8-70`, `i/.htaccess:1-9`

当前上传入口通过扩展名、MIME和内容扫描拒绝新 SVG，但公开 `thumb.php` 在解析到存储根内的 `.svg` 文件后，明确设置 `Content-Type:image/svg+xml` 并把原字节原样输出，不做安全转换或附件下载。上传目录规则只阻止脚本后缀，不阻止 SVG。项目曾允许/尝试净化 SVG，升级不会扫描已有图片目录，因此旧版本上传的恶意 SVG会继续存在。

访问者直接打开 `/app/thumb.php?img=...svg` 时，SVG作为本站同源文档加载；当前 CSP允许本站脚本且包含 `'unsafe-inline'`，SVG内的脚本/事件处理器可读取同源数据并在管理员打开链接时使用其 Session。即使新上传封堵正确，历史公开链接、备份恢复或未受统一验证的归档导入仍会重新暴露该存量风险。

**修复要求**：缩略图端永不原样返回 SVG；对 SVG统一返回 415/安全占位图，或在隔离进程中光栅化成 PNG并验证输出。升级任务递归扫描存储、缓存、回收和隔离目录，按内容识别并隔离全部 SVG/伪装 SVG，生成可审计报告；静态服务器也应对 SVG使用附件下载或独立无 Cookie域名。增加历史恶意 SVG、大小写/双扩展、伪装内容、归档恢复、直接与缩略图访问及迁移幂等测试。

### 75. 中危 — 本地压缩和水印直接覆盖正式原图且忽略编码结果，写入失败会静默损坏唯一副本

**文件**: `app/compress/Imagick/class.Imgcompress.php:34-55`, `app/compress/Imagick/class.Imgcompress.php:95-114`, `app/WaterMask.php:325-355`, `app/function.php:2094-2156`, `app/compress/function.compress.php:20-34`

所谓 Imgcompress先把源图解码到内存，再调用 `imagejpeg/png/...` 将结果写回同一个源路径；水印器也按目标扩展直接把 GD画布写回 `$source`。两者都不写临时文件、不保留备份，也不检查编码函数布尔返回、最终字节数、图片能否重新解码或尺寸是否符合预期。正式文件在新编码完整验证前就被截断/替换。

磁盘写满、配额、权限变化、编码器错误或进程中止时，原图可能变成零字节或部分图片。上传流程以 `@` 调用并已向客户端返回成功，批量压缩则每项无条件输出路径并最终显示“压缩完毕”，所以失败既无法反馈也无法自动恢复。开启 FTP本地删除时还可能把损坏版本传到远端或删除剩余本地副本。

**修复要求**：所有变换输出到原文件同目录的随机临时文件，检查编码返回和完整写入，重新用 `finfo`、`getimagesize()`及严格解码验证格式/尺寸，`fflush`/`fsync`后原子替换；失败删除临时文件并保留原图。处理函数返回结构化结果，上传发布事务和批处理逐项处理失败，必要时保留可恢复备份。增加磁盘满、权限/短写、编码失败、进程中止、坏输入、FTP组合和成功原子替换测试。

### 76. 中危 — 远程附件默认使用明文 FTP，上传服务器高权限凭据和全部图片可被链路窃取

**文件**: `config/config.php:17-27`, `admin/admin.inc.php:1462-1524`, `app/function.php:2513-2548`

远程附件功能使用 FTP账号执行读取、写入、删除及建目录。配置默认 `ftp_ssl=0`、端口 21；管理员勾选“启用远程附件”时不会联动开启加密或阻止保存，只另有一个默认关闭的“启用SSL”开关。运行时该分支调用普通 `ftp_connect`，随后以明文协议发送用户名、密码、目录命令和图片内容。

同网段、运营商、恶意代理或被入侵网络设备可被动获取具有删除权限的长期 FTP凭据及所有原图，也可主动篡改传输。图片可能原本只通过 HTTPS访问，FTP回源却取消了链路保护；开启“完成删除”后远端甚至成为唯一副本，窃取的账号可直接删除或替换全部对象。界面没有显著说明这一后果，也不验证加密会话身份。

**修复要求**：新配置只允许有主机身份验证的 SFTP或经验证证书的 FTPS，默认拒绝明文 FTP；确需兼容时必须显式启用高风险旧模式、限制到受控网络且使用最小权限独立账号。秘密从源码配置迁到环境/密钥存储并支持轮换，传输后校验大小/哈希。增加明文模式默认拒绝、证书/主机密钥错误、降级尝试、最小权限、凭据轮换和端到端加密上传测试。

### 77. 中危 — “自动删除”依赖管理员恰好每天访问后台，错过日期的图片将永久绕过保留策略

**文件**: `admin/admin.inc.php:311`, `admin/admin.inc.php:597-606`, `app/function.php:2324-2371`

自动删除没有计划任务或请求外的调度器，只在已登录管理员打开设置页时调用。每次运行也不扫描截止日期以前的目录，而是只构造“恰好 N天前”目录进行重命名，以及“恰好 2N天前且已重命名”目录进行删除。若管理员某天未访问后台，那一天对应的原目录此后不再匹配任何一次计算，将永久保留；删除阶段错过时，`_auto_delete`目录同样永久残留。

界面 tooltip虽写着“每天需要访问一次后台”，但这使数据保留/隐私策略依赖人工无缺勤操作，不能称为自动。函数不检查 `rename()`、目录/日志创建和日志写入结果，却固定返回 true；只保留最后一个 `$log`，FTP远端也完全不执行同步重命名/删除，所以管理员无法证明策略实际生效，远端副本可无限期存在。

**修复要求**：由 cron/队列运行幂等保留任务，枚举并处理所有日期 `<= cutoff` 的规范目录，而不是只算一个精确日期；使用持久任务状态和逐对象结果，失败可重试且不覆盖旧日志。明确本地、缓存、回收、隔离及远端存储的一致保留语义，FTP/SFTP操作确认成功后再推进状态。增加停跑多日后补偿、跨月/年、部分失败重试、并发任务、远端离线和审计报告测试。

### 78. 低危 — API上传日志从未记录 Token ID，后台把全部 API请求误标为网页上传

**文件**: `api/index.php:208-214`, `app/function.php:2200-2222`, `app/viewlog.php:55-85`, `app/info.php:117-120`

`write_upload_logs()` 的第四个参数 `$from` 默认为字符串 `"web"`，日志查看器又用“是否为字符串”判断来源：字符串显示网页，数值显示 API Token ID。网页入口使用默认值合理，但 API入口同样只传前三个参数，虽然当前请求已经取得 `$tokenID`，却从未传入日志函数。因此每一条 API上传都持久化为 `from=web`。

管理员无法按来源调查滥用、定位应吊销的 Token或区分网页/API容量；详情页也固定显示“通过网页”。如果公开已知默认 Token（第 68 项）或某个 Token泄漏，审计记录会错误指向匿名网页入口，延误处置。日志字段命名和展示逻辑还依赖值类型而非明确事件 schema，字符串形式的数字 ID也会再次被误判。

**修复要求**：API入口显式传递不可变 Token ID或独立凭据记录 ID；日志使用明确字段，例如 `channel: web|api`、`credential_id`，不要以 PHP类型编码业务含义。网页与 API共用事件写入器并校验必需字段，敏感 Token只记录不可逆标识。增加网页上传、多个 API Token、数字字符串 ID、Token吊销追踪和日志迁移测试。


### 79. 中危 — 月度上传日志以 basename 作为唯一键，同名图片会静默覆盖历史审计记录

**文件**: `app/function.php:2200-2250`, `app/viewlog.php:55-87`

每条日志使用 `basename($filePath)` 作为 `$logs` 数组键，再通过 `array_replace($logs, $log)` 写回整个月文件。键中没有日期目录、用户/Token目录、上传时间或事件 ID。同一个月内，只要两个上传最终 basename 相同，后一次就直接替换前一次；例如按原名命名时，不同日期目录中的 `photo.jpg`，或者不同访客/API Token隔离目录中生成相同文件名，都不会因完整路径不同而保留两条事件。

这会永久丢失旧记录的来源 IP、时间、原始名称、哈希和凭据归属，后台又没有覆盖告警。文件仍实际存在于不同目录，图片统计与审计表因此互相矛盾；攻击者也可通过重复可控名称降低违规上传的可追溯性。即使修复第 35、65 项的并发/原子写入，这个确定性的业务键冲突仍然存在。

**修复要求**：日志改为追加型事件模型，每次上传生成密码学随机或单调唯一的事件 ID，并把规范完整存储路径作为普通字段而非数组主键；数据库不可用时可使用 JSON Lines/按日分片并以 `LOCK_EX` 完整追加，另建可重建索引。迁移旧日志时保留现有键并标记无法恢复的覆盖风险。增加同月跨日同名、不同访客/Token同名、并发同名、重命名冲突和后台逐条展示测试。

### 80. 中危 — 公开密码散列工具可被匿名反复调用，昂贵的 `password_hash()` 会耗尽 PHP worker CPU

**文件**: `app/reset_password.php:1-8`, `admin/index.php:113-126`

忘记密码帮助把所有访问者引向公开的 `/app/reset_password.php`。该页面不要求管理员会话、CSRF、一次性恢复凭据或任何速率限制；任意请求只要提交 8 至 128 字节的 `password`，服务端就执行一次 `password_hash(..., PASSWORD_DEFAULT)`。在当前 PHP 上这通常是故意昂贵的 bcrypt/Argon散列，攻击者可保持大量并发 POST，让每个 FPM worker持续占用 CPU。

这个端点没有上传 API的 Token/IP限制，也没有业务上必须在线提供散列服务的理由。攻击只需极小入站带宽即可提高整站响应延迟或耗尽 worker池，使登录、上传和管理操作不可用；将来 PHP默认散列成本提高时风险会自动放大。密码长度上限只约束输入内存，不约束请求频率或并发计算总量。

**修复要求**：移除公开 Web散列器，改为仅供本机 CLI执行的恢复命令；如必须保留页面，则要求已认证管理员或高熵、短期、一次性恢复令牌，并实施严格的 IP/令牌速率、全局并发上限和请求超时。恢复过程应原子更新凭据、注销旧 Session并留下审计事件，而不是让用户手改 PHP源码。增加匿名拒绝、高并发、令牌一次性/过期、散列失败和恢复后旧会话失效测试。

### 81. 低危 — 登录验证码校验后不失效，同一个答案可在 Session 内无限重放

**文件**: `app/captcha.php:13-25`, `admin/index.php:43-83`

验证码图片生成时把小写答案写入 `$_SESSION['code']`，登录处理只做字符串比较；无论验证码正确后密码成功还是失败，都没有 `unset()`、轮换、签发时间或已用标记。浏览器显示新页面时通常会再次加载图片并偶然覆盖答案，但直接 HTTP客户端不需要加载页面：获取并识别一次验证码后，可在同一 Session的所有后续登录 POST中重复提交这个答案。

因此启用 CAPTCHA不能阻止自动密码尝试，只剩独立登录限流生效；分布式来源仍可各自求解一次后长期复用。验证码也没有有效期，泄漏的 Session文件/答案在 Session 14 天生命周期内可能持续有效。当前字符使用非密码学 `rand()`，但主要缺陷是挑战完全不消费，即使换成安全随机数也仍可重放。

**修复要求**：在任何校验尝试时先原子取出并删除验证码，正确也只能使用一次；记录短有效期、用途和会话绑定，使用 `random_int()`生成，比较时处理缺失状态且不泄露差异。失败页面需显式要求新挑战，并让速率限制与 CAPTCHA互补而非互相替代。增加正确答案重放、错误后重试、过期、并发使用同一答案、跨 Session和不加载新图片的直接 POST测试。

### 82. 中危 — 文件管理器保存界面设置时原地重写自身 PHP源码，中断或短写会破坏整个入口

**文件**: `admin/manager.php:539-583`, `admin/manager.php:3321-3367`

`FM_Config::save()` 没有写独立配置文件，而是先读取 `__FILE__` 全部行，再以 `fopen($fm_file, "w")` 截断当前正在执行的 `admin/manager.php`，写入新的前三行后逐行拼回剩余源码。没有锁、临时文件、完整写检查或语法校验，且 `fopen`/`fputs` 全部用 `@` 隐藏错误；上层无论是否真正保存都固定 `echo true`。

磁盘满、配额、短写、进程终止或两个管理员请求并发保存时，可留下只有配置头、半截源码或交错版本的 180 KiB PHP文件，下一次访问直接语法错误/功能残缺。与第 65 项的 `config/*.php` 写入不同，这条路径不调用 `cache_write()`，即使统一修复主配置发布也不会覆盖；它还要求应用源代码对 Web用户可写，扩大运行时被篡改的范围。

**修复要求**：将管理器 UI状态移到专用、非可执行数据文件，并让部署源码只读；通过 schema解析 JSON，采用锁文件覆盖读取-修改-发布事务及同目录临时文件原子替换。调用者必须检查结果并返回真实 HTTP/JSON错误，不能抑制 I/O失败。增加并发保存、短写/磁盘满、进程中止、源码只读、坏 JSON恢复和保存后 PHP源码哈希不变测试。
